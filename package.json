{
  "name": "better-cassandra",
  "description": "A better way to work with cassandra! (Also supports scylladb)",
  "version": "1.1.1",
  "main": "lib/index",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --clean",
    "test": "nodemon --exec ts-node ./test/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "cassandra-driver": "^4.7.2"
  },
  "files": [
    "lib"
  ]
}
